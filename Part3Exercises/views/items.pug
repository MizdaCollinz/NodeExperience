extends base.pug

block content
    h1 Here are the items
    each item in items
        p [Name: #{item.name}] , [Price: #{item.price}]
    br
    a(href="/items/new") Add new items to the cart
    form(action="/items?_method=DELETE" method="POST")
        input(type="submit" value="Clear the Cart")